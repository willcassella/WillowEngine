# Travis CI configuration file
language: c++
compiler:
    - clang
    - gcc
sudo: required
dist: trusty
install:
    - ./travis-packages.sh
script: 
    - mkdir build && cd build
    - cmake ../
    - make
